# The watched regions for boot-idle: the two boot RNG states
# (pins/arm9.tsv data pins — both zeroed bss until the game seeds them).
# sMTRNG_State is 2496 bytes, so it samples coarser to cap trace size.
# name         bucket  address     size   sample
sLCRNG_State   hard    0x021D15A8  4      1
sMTRNG_State   hard    0x021D15AC  2496   30