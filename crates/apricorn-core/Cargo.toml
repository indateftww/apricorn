[package]
name = "apricorn-core"
description = "Headless HeartGold game engine and logic (no platform code)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Intentionally dependency-free at this stage; platform and heavy crates
# belong in apricorn-gfx / apricorn-audio / apricorn-desktop.

[lints]
workspace = true